using UnityEngine;

namespace UnityTestRedRift.Util
{
    public class CardTransformCalculator
    {
        public Vector3 CalcPosition(int index, int cardCount)
        {
            return Vector3.zero;
        }
        
        
        public Quaternion CalcRotation(int index, int cardCount)
        {
            return Quaternion.identity;
        }
    }
}